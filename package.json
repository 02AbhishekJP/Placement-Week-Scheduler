{
  "name": "placement-week-scheduler",
  "version": "1.0.0",
  "description": "Placement Week Scheduler — 35 companies, 800 students, 20 rooms, 4 days",
  "private": true,
  "scripts": {
    "dev:server": "cd server && node src/index.js",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "db:generate": "cd server && npx prisma generate",
    "db:push": "cd server && npx prisma db push",
    "db:studio": "cd server && npx prisma studio",
    "setup": "npm install && cd server && npm install && npx prisma generate && npx prisma db push && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
